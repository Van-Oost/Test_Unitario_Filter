
function filter(miArray) {
    var arrayFiltrado = [];
    for (i= 0; i < miArray.length; index++) {
        if (miArray[i]>5){
            arrayFiltrado.push(number);
        };
    return arrayFiltrado;
    };
};

